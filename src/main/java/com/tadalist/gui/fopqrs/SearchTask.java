package com.tadalist.gui.fopqrs;

import javax.swing.*;
import java.awt.*;

public class SearchTask extends JPanel {
    public SearchTask() {
        // Set layout and content for Page1
        this.setLayout(new BorderLayout());
        JLabel label = new JLabel("search task", SwingConstants.CENTER);
        this.add(label, BorderLayout.CENTER);

    }
}