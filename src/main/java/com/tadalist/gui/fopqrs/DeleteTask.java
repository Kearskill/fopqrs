package com.tadalist.gui.fopqrs;

import javax.swing.*;
import java.awt.*;

public class DeleteTask extends JPanel {
    public DeleteTask() {
        // Set layout and content for Page1
        this.setLayout(new BorderLayout());
        JLabel label = new JLabel("delete task page", SwingConstants.CENTER);
        this.add(label, BorderLayout.CENTER);

    }
}