package com.tadalist.gui.fopqrs;

import javax.swing.*;
import java.awt.*;

public class VectorSearch extends JPanel {
    public VectorSearch() {
        // Set layout and content for Page1
        this.setLayout(new BorderLayout());
        JLabel label = new JLabel("vector search", SwingConstants.CENTER);
        this.add(label, BorderLayout.CENTER);

    }
}